#ifndef _FIELDTYPE_HPP
#define _FIELDTYPE_HPP

/*
 * Descriptor enum for the content of a map.
 */

enum eFieldType
{
	EMPTY_FIELD,
	WALL_FIELD,
	PATH_FIELD // only for output
};

#endif // _FIELDTYPE_HPP